{
  "buildCommand": "cd frontend && npx --yes pnpm@10 install --no-frozen-lockfile && npx --yes pnpm@10 run build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "functions": {
    "api/index.py": {
      "includeFiles": "backend/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ]
}
